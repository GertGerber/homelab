# Cache objects
packer_cache/

# Crash log
crash.log

# https://www.packer.io/guides/hcl/variables
# Exclude all .pkrvars.hcl files, which are likely to contain sensitive data, 
# such as password, private keys, and other secrets. These should not be part of 
# version control as they are data points which are potentially sensitive and 
# subject to change depending on the environment.
#
*.pkrvars.hcl

# For built boxes
*.box

# Packer build artifacts
*.box
*.ova
*.vmdk
*.vhd
*.qcow2
*.ami

# Packer cache and temporary files
.packer/
packer_cache/

# Sensitive files (e.g., credentials, API keys)
*.json.secret
credentials.json
ansible/vault.yml
packer/secret.json

# Logs and other temporary output
*.log
output/

# OS and editor specific files
.DS_Store
Thumbs.db
.vscode/
.idea/

# **************************************************************
# Local .terraform directories
**/.terraform/*

# .tfstate files (containing sensitive data)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# passwords, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
example.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in.
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform plan files
*.tfplan
*.tfplan.binary

# Terraform CLI configuration files
.terraformrc
terraform.rc

# Environment-specific variable files that should not be committed
# For example, if you have a file named `dev.tfvars` or `prod.tfvars` that
# contains sensitive or environment-specific values, you might list them here.
dev.tfvars
prod.tfvars

# If you are using a Terraform lock file and want to commit it for consistent provider versions across environments,
# do NOT include the following line. Otherwise, uncomment to ignore it.
.terraform.lock.hcl

# **************************************************************
# Ansible temporary files
*.retry
*.log
*.tmp
.ansible/
ansible-tmp-*

# Inventory related
# It's often recommended to keep sensitive inventory files out of version control,
# or to manage them securely outside the main repository.
# If you manage inventories separately (e.g., symlinking), ignore the symlinked path.
# inventories/production
# inventories/staging

# Python related (if using Python for custom modules or plugins)
*.pyc
__pycache__/
.venv/
venv/

# Editor/IDE specific files
.idea/
.vscode/
*.swp
*.swo
*~

# Sensitive files (e.g., vault passwords, private keys)
# NEVER commit these to your repository.
# Ensure these are managed securely, e.g., using Ansible Vault and environment variables.
vault_password.txt
*.pem
*.key
*.crt

# Build artifacts or generated files
build/
dist/

# Collections (if managing them locally and not committing them)
collections/ansible_collections

# Other common ignores
.DS_Store
Thumbs.db